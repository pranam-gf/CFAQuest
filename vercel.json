{
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/server"
    },
    {
      "source": "/(.*)",
      "destination": "/client/dist/$1"
    }
  ],
  "builds": [
    {
      "src": "client/**",
      "use": "@vercel/static-build",
      "config": { "outputDirectory": "client/dist" }
    },
    {
      "src": "server/index.ts",
      "use": "@vercel/node"
    }
  ]
}