export const formatContextLength = (length: number): string => {
  return `${length}`;
};

export const getContextLengthColor = (length: number): string => {
  return "text-gray-900 dark:text-white";
};